define service {
    use							generic-service
    host_name					{{ host }}
    service_description			OAM_{{ host }}_{{ maintenance_domain }}
    check_command				dummy
    passive_checks_enabled  	1
    active_checks_enabled   	0
}

