define host {
        use                     generic-host            ; Name of host template to use
        host_name               {{ hostname }}
        alias                   {{ hostname }}
        address                 {{ address }}
}
